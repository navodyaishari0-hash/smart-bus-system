{
  "name": "smart-bus-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "tunnel": ".\\cloudflared.exe tunnel --url http://localhost:5000",
    "tunnel:frontend": ".\\cloudflared.exe tunnel --url http://localhost:5173",
    "backend": "node backend/server.js",
    "frontend": "npm run dev --prefix frontend",
    "init-db": "node backend/init-db.js"
  }
}
